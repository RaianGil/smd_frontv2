export interface GTConfig {
    _id?: string,
    bot_name: String;
    crypto_symbol: string;
    max_price: string;
    min_price: string;
    grid_number: string;
    grid_percent: string;
    amount_limit: string;
    bot_active: string;
}